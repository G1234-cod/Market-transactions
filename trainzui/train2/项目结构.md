trainzui/train2/
│
├── convert_kaputt.py              ← 格式转换（mask→YOLO bbox）
├── train.py                       ← 训练脚本
├── requirements.txt               ← Python 依赖
│
├── dataset/                       ← 原始数据（你放进去）
│   ├── data.yaml                  ←   类别模板（参考）
│   │
│   ├── data/                      ←   query-image.tar.gz 解压出来
│   │   ├── train/query-data/
│   │   │   ├── image/*.jpg        ←   2048×2048 缺陷图片
│   │   │   ├── crop/*.jpg
│   │   │   └── mask/*.png         ←   分割掩码
│   │   ├── validation/query-data/
│   │   │   └── ...
│   │   └── test/query-data/
│   │       └── ...
│   │
│   └── datasets/                  ←   datasets.tar.gz 解压出来
│       ├── query-train.parquet
│       ├── query-validation.parquet
│       ├── query-test.parquet
│       ├── reference-*.parquet    ←   不用管
│       └── README.md
│
├── photo/                         ← convert 后自动生成
│   ├── data.yaml                  ←   train.py 读这个
│   ├── images/
│   │   ├── train/*.jpg            ←   选中的训练图
│   │   └── val/*.jpg              ←   选中的验证图
│   └── labels/
│       ├── train/*.txt            ←   YOLO 标签
│       └── val/*.txt
│
├── runs/train_defect/             ← 训练自动生成
│   └── defect_detector/weights/
│       ├── best.pt
│       └── last.pt
│
├── defect_best.pt                 ← 训练完拷贝出来的模型
│
└── .venv/                         ← 虚拟环境


以后想换大小就改数字就行：
python convert_kaputt.py --max-gb 0.5   # 500MB
python convert_kaputt.py --max-gb 2.5   # 2.5GB
python convert_kaputt.py                # 全部数据
