# 项目版本迭代记录

## 📋 迭代总览

| 版本 | 日期 | 核心变化 | 状态 |
|:---:|:---:|:---|:---:|
| v0.1 | 2026-06-01 | 项目初始化、需求分析、技术选型 | ✅ 完成 |
| v0.2 | 2026-06-03 | 数据库设计、后端框架搭建 | ✅ 完成 |
| v0.3 | 2026-06-06 | 用户系统、JWT 认证、Qwen 识别集成 | ✅ 完成 |
| v0.4 | 2026-06-09 | YOLO 检测、画框标注 | ✅ 完成 |
| v0.5 | 2026-06-12 | 瑕疵检测、程度分级 | ✅ 完成 |
| v0.6 | 2026-06-15 | 图像预处理、双模型比对 | ✅ 完成 |
| v0.7 | 2026-06-18 | DeepSeek 定价、发布历史 | ✅ 完成 |
| v0.8 | 2026-06-20 | 前端页面开发（6 个页面） | ✅ 完成 |
| v0.9 | 2026-06-22 | 以图搜图、向量数据库 | ✅ 完成 |
| v0.10 | 2026-06-24 | 错误数据收集、数据飞轮 | ✅ 完成 |
| v0.11 | 2026-06-25 | 接口联调、测试通过 | ✅ 完成 |
| v0.12 | 2026-06-26 | 代码重构、Bug 修复、配置统一 | ✅ 完成 |
| v0.13 | 2026-06-27 | 环境配置自动化、依赖管理 | ✅ 完成 |
| v0.14 | 2026-06-28 | 模型训练脚本优化 | ✅ 完成 |
| v1.0 | 待定 | 模型训练完成、部署上线 | ⏳ 进行中 |


## 📝 详细迭代记录


### v0.1 —— 项目初始化（2026-06-01）

**新增功能：**

- 项目立项，明确需求目标
- 技术选型（FastAPI + Vue 3 + YOLOv8 + CLIP + Qwen + DeepSeek）
- 项目目录结构搭建
- 需求文档编写（项目总体设计说明书）

**技术决策：**

- 确定 FastAPI 作为后端框架（原生 async/await 支持 SSE）
- 确定 Vue 3 + Vite + TailwindCSS 作为前端技术栈
- 确定 YOLOv8 作为目标检测模型
- 确定 Qwen-VL-Max + DeepSeek 双大模型方案
- 确定 CLIP + Qdrant 作为以图搜图方案
- 确定 MySQL 8.0 作为主数据库

**新增文件：**

- `项目文档/项目总体设计说明书.md`
- `backend/requirements.txt`
- `backend/.env.example`
- `frontend/package.json`
- `README.md`
- `.gitignore`

**里程碑：** 项目正式启动，技术方案确定


### v0.2 —— 数据库设计（2026-06-03）

**新增功能：**

- MySQL 数据库完整设计（8 张表）
- 数据库连接池管理（aiomysql 异步连接池）
- 基础 CRUD 操作实现
- 测试数据插入（20+ 条行情数据）

**新增文件：**

- `backend/app/db/connection.py`（连接池管理）
- `backend/app/db/crud.py`（CRUD 操作）
- `backend/sql/schema.sql`（完整建表语句）
- `backend/sql/seed.sql`（测试数据）

**数据库表：**

| 表名 | 用途 | 索引 |
|:---|:---|:---|
| users | 用户信息 | username UNIQUE, status |
| market_prices | 二手行情 | brand_model UNIQUE, category |
| published_items | 商品发布 | user_id, status, category, created_at |
| ai_audit_logs | AI 审计日志 | user_id, status, action_type, created_at |
| item_likes | 点赞记录 | item_user UNIQUE |
| hard_cases | 错题本 | wrong_label, correct_label, is_fixed |
| feature_vectors | 特征向量（备用） | item_id UNIQUE |
| price_history | 价格历史 | brand_model, recorded_at |

**里程碑：** 数据库设计完成，可支撑全部业务功能


### v0.3 —— 用户系统 + JWT 认证 + Qwen 识别（2026-06-06）

**新增功能：**

- 用户注册（PBKDF2-SHA256 密码哈希，100,000 迭代）
- 用户登录（JWT Token 签发与验证）
- Qwen-VL-Max 视觉识别集成
- 图片上传与本地保存
- 结构化 JSON 输出解析
- 审计日志记录

**新增文件：**

- `backend/app/routers/auth.py`（认证路由）
- `backend/app/routers/extract.py`（识别路由）
- `backend/app/llm/qwen_vl_client.py`（Qwen 客户端）
- `backend/app/llm/base.py`（LLM 抽象基类）
- `backend/app/auth/jwt_handler.py`（JWT 处理）
- `backend/app/models/schemas.py`（Pydantic 模型）
- `backend/app/services/audit_service.py`（审计服务）
- `backend/app/services/vision_service.py`（视觉服务）

**API 新增：**

| 接口 | 方法 | 功能 | 认证 |
|:---|:---:|:---|:---:|
| `/api/v1/register` | POST | 用户注册 | ❌ |
| `/api/v1/login` | POST | 用户登录（返回 JWT） | ❌ |
| `/api/v1/extract` | POST | Qwen 视觉识别 | ✅ |

**安全措施：**

- PBKDF2-SHA256 密码哈希（100,000 迭代 + 16 字节盐）
- JWT Bearer Token（HS256 签名，30 分钟过期）
- 文件上传类型/大小校验

**里程碑：** 用户系统 + AI 识别基础链路打通


### v0.4 —— YOLO 检测 + 画框标注（2026-06-09）

**新增功能：**

- YOLOv8 预训练模型加载（yolov8n.pt）
- 物品检测（80 类 COCO 数据集）
- 多色画框标注
- Base64 图片输出（前端直显）
- 置信度显示

**新增文件：**

- `backend/app/ml/yolo_detector.py`
- `backend/app/routers/detect.py`

**API 新增：**

| 接口 | 方法 | 功能 | 认证 |
|:---|:---:|:---|:---:|
| `/api/v1/yolo/detect` | POST | YOLO 物品检测 | ✅ |
| `/api/v1/yolo/health` | GET | 健康检查 | ❌ |

**颜色映射：**

| 类别 | 颜色 | 十六进制 |
|:---|:---|:---|
| phone | 红色 | #FF6B6B |
| laptop | 青色 | #4ECDC4 |
| tablet | 蓝色 | #45B7D1 |
| camera | 绿色 | #96CEB4 |
| headphone | 黄色 | #FFEAA7 |
| watch | 紫色 | #DDA0DD |
| default | 红色 | #FF6B6B |

**里程碑：** 本地模型检测能力接入


### v0.5 —— 瑕疵检测 + 程度分级（2026-06-12）

**新增功能：**

- 瑕疵检测（5 类：划痕/磕碰/污渍/裂痕/掉漆）
- 不同形状+颜色代表不同程度（视觉编码）
- 标注图生成（PNG 格式）
- 程度分级内部逻辑（不告知用户）
- 结构化瑕疵数据输出

**新增文件：**

- `backend/app/ml/defect_detector_yolo.py`
- `backend/app/routers/process.py`
- `backend/app/utils/image_utils.py`

**程度分级系统：**

| 形状 | 颜色 | 十六进制 | 程度 | 代表含义 |
|:---|:---|:---|:---|:---|
| 圆形 | 红色 | #FF0000 | 重度 | 影响功能/结构 |
| 矩形 | 橙色 | #FF8C00 | 中度 | 影响外观 |
| 多边形 | 黄色 | #FFD700 | 轻度 | 细微痕迹 |
| 虚线框 | 蓝色 | #00BFFF | 轻微 | 表面污渍 |

**瑕疵类别映射：**

| ID | 英文 | 中文 | 默认程度 |
|:---|:---|:---|:---|
| 0 | scratch | 划痕 | minor |
| 1 | dent | 磕碰 | moderate |
| 2 | stain | 污渍 | slight |
| 3 | crack | 裂痕 | severe |
| 4 | other | 其他 | minor |

**里程碑：** 瑕疵检测 + 分级标注完成


### v0.6 —— 图像预处理 + 双模型比对（2026-06-15）

**新增功能：**

- 图像预处理管道（尺寸统一 448×448）
- 多物体判断（选占比最大的物体）
- 去背景（rembg 集成）
- 去噪（OpenCV 高斯滤波）
- 双模型并行识别（YOLO + Qwen）
- 结果比对 + 不一致数据收集
- 错误数据本地保存

**新增文件：**

- `backend/app/utils/preprocess.py`
- `backend/app/ml/data_collector.py`
- `backend/app/utils/font_utils.py`
- `backend/app/utils/file_validator.py`

**数据流转：**

```
用户上传图片
    ↓
尺寸统一 (448×448)
    ↓
去背景 (rembg) + 去噪 (OpenCV)
    ↓
┌─────────────────┴─────────────────┐
│                                   │
YOLO 检测                          Qwen 识别
│                                   │
└─────────────────┬─────────────────┘
                  ↓
            结果比对
                  ↓
         ┌───────┴───────┐
         │               │
      一致          不一致 → 收集错误数据
         │               │
         ↓               ↓
     正常返回      error_data/images/
                  error_data/labels/
```

**里程碑：** 数据飞轮第一步——错误数据收集


### v0.7 —— DeepSeek 定价 + 发布历史（2026-06-18）

**新增功能：**

- DeepSeek 智能定价（结合瑕疵程度 + 市场行情）
- 三层查价匹配策略（精确→模糊→空）
- 商品发布历史管理
- 商城商品列表展示
- 下架/重新上架功能
- SSE 流式文案生成（打字机效果）

**新增文件：**

- `backend/app/llm/deepseek_price_client.py`
- `backend/app/routers/price.py`
- `backend/app/routers/history.py`
- `backend/app/routers/market.py`
- `backend/app/routers/generate.py`
- `backend/app/services/price_service.py`
- `backend/app/services/text_service.py`

**API 新增：**

| 接口 | 方法 | 功能 | 认证 |
|:---|:---:|:---|:---:|
| `/api/v1/price` | GET | 行情查询（三层匹配） | ✅ |
| `/api/v1/history` | GET | 发布历史 | ✅ |
| `/api/v1/history/save` | POST | 保存商品 | ✅ |
| `/api/v1/history/{id}/delist` | POST | 下架商品 | ✅ |
| `/api/v1/history/{id}/publish` | POST | 重新上架 | ✅ |
| `/api/v1/market` | GET | 商城列表 | ❌ |
| `/api/v1/generate` | POST | SSE 流式生成文案 | ✅ |

**三层匹配策略：**

1. 精确匹配：`WHERE brand=%s AND model=%s`
2. 模糊匹配：`difflib.SequenceMatcher` 相似度 > 0.7
3. 返回空：标注"暂无市场行情数据"

**里程碑：** 完整发布流程打通（识别→检测→定价→保存）


### v0.8 —— 前端页面开发（2026-06-20）

**新增页面（6 个）：**

| 页面 | 路由 | 功能 | 认证 |
|:---|:---|:---|:---:|
| 登录页 | /login | 用户登录 | ❌ |
| 注册页 | /register | 用户注册 | ❌ |
| 发布页 | / | 三步流程（上传→确认→生成） | ✅ |
| 商城页 | /market | 商品列表 + 搜索 + 筛选 | ❌ |
| 历史页 | /history | 发布记录 + 草稿管理 | ✅ |
| 详情页 | /detail/:id | 商品详情 | ❌ |

**新增组件（6 个）：**

| 组件 | 功能 |
|:---|:---|
| ImageUploader.vue | 图片上传（拖拽 + 点击） |
| ConfirmCard.vue | 信息确认（三级校验） |
| DefectViewer.vue | 瑕疵标注展示 |
| SearchByImage.vue | 以图搜图组件 |
| HistoryCard.vue | 历史记录卡片 |
| TypewriterText.vue | 打字机文案展示 |

**前端技术：**

- Vue 3 Composition API + script setup
- Vue Router 4（路由守卫）
- Pinia（用户状态管理）
- TailwindCSS（原子化样式）
- 响应式设计（移动端适配）

**里程碑：** 前端页面全部开发完成


### v0.9 —— 以图搜图 + 向量数据库（2026-06-22）

**新增功能：**

- CLIP 特征提取器（open-clip-torch）
- Qdrant 向量数据库（Docker 部署）
- 以图搜图（上传图片 → 特征提取 → 向量检索 → 返回相似）
- 以文搜图（输入文字 → 文本编码 → 向量检索 → 返回相似）
- 商品索引自动维护（发布时索引、下架时删除）

**新增文件：**

- `backend/app/ml/clip_extractor.py`
- `backend/app/ml/qdrant_client.py`
- `backend/app/ml/faiss_index.py`（备用）
- `backend/app/routers/search.py`
- `scripts/index_items.py`
- `docker/docker-compose.yml`

**API 新增：**

| 接口 | 方法 | 功能 | 认证 |
|:---|:---:|:---|:---:|
| `/api/v1/search/image` | POST | 以图搜图 | ✅ |
| `/api/v1/search/text` | POST | 以文搜图 | ✅ |
| `/api/v1/search/index` | POST | 加入索引 | ✅ |
| `/api/v1/search/stats` | GET | 索引统计 | ✅ |
| `/api/v1/search/health` | GET | 健康检查 | ❌ |

**向量检索流程：**

```
用户上传图片/输入文字
    ↓
CLIP 提取特征向量 (512维)
    ↓
Qdrant 向量检索 (COSINE 相似度)
    ↓
返回 Top-K 商品ID
    ↓
MySQL 查询商品详情
    ↓
返回相似商品列表
```

**Docker 启动：**

```bash
docker run -d -p 6333:6333 -v ./data/qdrant:/qdrant/storage qdrant/qdrant
```

**里程碑：** 以图搜图完整功能上线


### v0.10 —— 错误数据收集 + 数据飞轮（2026-06-24）

**新增功能：**

- 错误数据自动收集（双模型不一致时）
- 错题本本地存储（images + labels + metadata）
- 每周训练脚本框架（weekly_train.py）
- 爬虫脚本框架（crawler.py）
- 数据库 hard_cases 表完善

**新增文件：**

- `scripts/weekly_train.py`
- `scripts/crawler.py`
- `app/ml/data_collector.py`
- `app/utils/background_tasks.py`

**数据飞轮闭环：**

```
用户使用 → 双模型识别
         ↓
    结果不一致 → 收集错误数据
         ↓
    每周定时训练 → 模型微调
         ↓
    模型更新 → 精度提升
         ↓
    (回到用户使用)
```

**错误数据存储结构：**

```
data/error_data/
├── images/          # 错误图片
│   └── item_123_20260624_143022.jpg
├── labels/          # 标签文件
│   └── item_123_20260624_143022.txt
└── metadata/        # 每周汇总
    └── error_summary_2026-W26.json
```

**里程碑：** 数据飞轮闭环建立，系统具备自进化能力


### v0.11 —— 接口联调 + 测试通过（2026-06-25）

**新增功能：**

- 单元测试框架（pytest + pytest-asyncio）
- 8 个测试文件，覆盖核心接口
- 所有 API 接口联调通过
- Swagger 文档自动生成
- FastAPI 生命周期管理

**测试文件：**

| 文件 | 测试内容 |
|:---|:---|
| test_auth.py | 注册/登录/认证 |
| test_extract.py | Qwen 识别 |
| test_detect.py | YOLO 检测 |
| test_process.py | 瑕疵检测 |
| test_search.py | 以图搜图 |
| test_price.py | 行情查询 |
| test_integration.py | 完整发布流程 |
| conftest.py | 测试配置 |

**测试结果：**

```
============================= test session starts =============================
collected 29 items
test/test_auth.py ...........                                            [ 37%]
test/test_extract.py ....                                                 [ 51%]
test/test_detect.py ...                                                   [ 62%]
test/test_process.py ....                                                 [ 75%]
test/test_search.py ...                                                   [ 86%]
test/test_integration.py ....                                             [100%]
============================= 29 passed in 12.14s =============================
```

**里程碑：** 所有接口测试通过，系统稳定


### v0.12 —— 代码重构 + Bug 修复 + 配置统一（2026-06-26）

**代码重构：**

- 配置统一管理（settings 单例）
- URL 工具函数统一（get_base_url / get_static_url）
- 单例模式线程安全修复（双重检查锁）
- 连接池管理优化（异步锁）
- JWT 认证完善（可选认证支持）
- 审计日志代码去重

**Bug 修复（20+ 项）：**

| 编号 | 问题 | 修复 |
|:---:|:---|:---|
| 1 | CORS 配置无效组合 | ✅ 使用 ALLOWED_ORIGINS |
| 2 | JWT 认证缺失 | ✅ 完整认证体系 |
| 3 | category 字段丢失 | ✅ 添加字段 |
| 4 | views/likes 未返回 | ✅ 添加查询 |
| 5 | condition vs item_condition | ✅ 统一为 condition |
| 6 | JSON 双编码 | ✅ safe_json_dumps |
| 7 | 硬编码 localhost | ✅ 动态 BASE_URL |
| 8 | 同步 requests 阻塞 | ✅ httpx.AsyncClient |
| 9 | top_k 无限 DoS | ✅ 限制 1-100 |
| 10 | RGBA 保存 JPEG | ✅ 转 RGB |
| 11 | 通用模型回退瑕疵检测 | ✅ 不降级 |
| 12 | 事件循环竞态 | ✅ 安全创建任务 |

**新增工具：**

- `app/utils/json_utils.py`（安全 JSON 序列化）
- `app/utils/background_tasks.py`（可靠后台任务）
- `app/auth/hash_utils.py`（统一密码哈希）

**里程碑：** 代码质量大幅提升，为生产部署做好准备


### v0.13 —— 环境配置自动化 + 依赖管理（2026-06-27）

**新增功能：**

- 环境配置自动检测脚本
- GPU/CPU 自动选择 PyTorch 版本
- 依赖自动安装与升级
- 文件编码自动检测
- 代理环境变量自动清理

**新增文件：**

- `环境依赖/torchzidong.py`（自动配置脚本）
- `环境依赖/verify_dependencies.py`（依赖验证）
- `requirements.txt`（完整依赖列表）

**智能特性：**

- 自动检测 NVIDIA GPU（nvidia-smi）
- 自动选择 CUDA/CPU PyTorch
- 自动检测 requirements.txt 编码
- 自动对比已安装版本
- 自动安装缺失包

**里程碑：** 新成员 5 分钟完成环境配置


### v0.14 —— 模型训练脚本优化（2026-06-28）

**新增功能：**

- 自动调参器（AutoTuner）实际应用
- 学习率/批次/Dropout 自动调整
- 断点续训语义修复
- 模型训练状态实时显示
- 中文训练日志

**修复问题：**

- AutoTuner 建议从未应用 → 已修复
- resume 语义错误（增量 vs 总目标）→ 已修复
- 训练设备自动检测 → 已实现
- GPU/CPU 自动选择 → 已实现

**训练脚本：**

| 脚本 | 用途 |
|:---|:---|
| train/train.py | 通用识别模型训练 |
| train/train2.py | 瑕疵检测模型训练 |
| scripts/weekly_train.py | 每周自动训练 |

**里程碑：** 模型训练流程完善，等待数据


## 📊 迭代统计

| 指标 | 数量 |
|:---|:---:|
| 总迭代次数 | 14 |
| 新增 Python 文件 | 45+ |
| 新增 Vue 文件 | 15+ |
| 新增 API 接口 | 18+ |
| 数据库表 | 8 |
| 测试文件 | 8 |
| AI 模型集成 | 5 |
| Bug 修复 | 25+ |
| 代码重构 | 10+ |


## 🚀 后续计划

| 版本 | 计划 | 预计日期 |
|:---:|:---|:---:|
| v1.0 | 兄弟训练完成 YOLO 模型，替换 best.pt 和 defect_best.pt | 2 周后 |
| v1.1 | 前端完整联调，全流程测试 | 模型到位后 2 天 |
| v1.2 | 云服务器部署（阿里云/腾讯云） | 联调完成后 2 天 |
| v1.3 | 每周自动训练脚本生产化 | 上线后 1 周 |
| v1.4 | 爬虫数据采集，扩充训练集 | 上线后 2 周 |
| v1.5 | 性能优化，Redis 缓存 | 上线后 3 周 |
| v1.6 | 监控告警系统（Prometheus + Grafana） | 上线后 4 周 |
| v2.0 | 移动端 App 适配（React Native） | 待定 |


## 💡 版本命名规则

```
v主版本.次版本.补丁

主版本：重大架构变更、不兼容升级
次版本：新功能添加、非破坏性变更
补丁：Bug 修复、性能优化、小调整
```


## 🏆 项目里程碑

| 里程碑 | 日期 | 说明 |
|:---|:---:|:---|
| 🚀 项目启动 | 2026-06-01 | 需求确定、技术选型 |
| 📊 数据库设计 | 2026-06-03 | 8 张表设计完成 |
| 🤖 AI 基础能力 | 2026-06-15 | Qwen + YOLO + 瑕疵检测 |
| 🔄 全链路打通 | 2026-06-18 | 发布流程完整 |
| 🎨 前端完成 | 2026-06-20 | 6 个页面全部开发 |
| 🔍 以图搜图 | 2026-06-22 | 向量检索上线 |
| 🌀 数据飞轮 | 2026-06-24 | 自进化闭环 |
| 🧪 测试通过 | 2026-06-25 | 29 个测试全部通过 |
| 🔧 代码重构 | 2026-06-26 | 20+ Bug 修复 |
| 📦 环境自动化 | 2026-06-27 | 一键配置 |
| 🎯 模型训练 | 待定 | YOLO 模型训练完成 |
| 🚀 正式上线 | 待定 | 生产环境部署 |